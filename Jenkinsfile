#!/usr/bin/env groovy

stage('compile') {
    // some block
    echo "Compile stage"
}
stage('testing') {
    // some block
    echo "Testing stage"
}
stage('deploy') {
    // some block
    echo "deploy stage"
}
